import { FC } from "react"

const Loading: FC = () => {
    return (
        <div className="flex flex-col h-screen justify-center items-center">
            <h1 className="text-1xl text-white mt-4">loading...</h1>
        </div>
    )
}

export default Loading
